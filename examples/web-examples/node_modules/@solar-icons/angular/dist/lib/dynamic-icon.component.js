import { Directive, effect, inject, input, signal, untracked, ViewContainerRef, } from '@angular/core';
import { SOLAR_ICON_REGISTRY } from './icon-registry';
import * as i0 from "@angular/core";
export class SolarIcon {
    vcr = inject(ViewContainerRef);
    registry = inject(SOLAR_ICON_REGISTRY, { optional: true });
    solarIcon = input.required(...(ngDevMode ? [{ debugName: "solarIcon" }] : []));
    size = input(...(ngDevMode ? [undefined, { debugName: "size" }] : []));
    color = input(...(ngDevMode ? [undefined, { debugName: "color" }] : []));
    strokeWidth = input(...(ngDevMode ? [undefined, { debugName: "strokeWidth" }] : []));
    weight = input(...(ngDevMode ? [undefined, { debugName: "weight" }] : []));
    secondaryColor = input(...(ngDevMode ? [undefined, { debugName: "secondaryColor" }] : []));
    secondaryOpacity = input(...(ngDevMode ? [undefined, { debugName: "secondaryOpacity" }] : []));
    alt = input(...(ngDevMode ? [undefined, { debugName: "alt" }] : []));
    ariaLabel = input(...(ngDevMode ? [undefined, { debugName: "ariaLabel" }] : []));
    titleAttr = input(...(ngDevMode ? [undefined, { debugName: "titleAttr" }] : []));
    componentRef = signal(undefined, ...(ngDevMode ? [{ debugName: "componentRef" }] : []));
    constructor() {
        effect(() => {
            const icon = this.solarIcon();
            this.render(icon);
        });
        effect(() => {
            const ref = this.componentRef();
            if (!ref)
                return;
            const size = this.size();
            const color = this.color();
            const strokeWidth = this.strokeWidth();
            const weight = this.weight();
            const secondaryColor = this.secondaryColor();
            const secondaryOpacity = this.secondaryOpacity();
            const alt = this.alt();
            const ariaLabel = this.ariaLabel();
            const titleAttr = this.titleAttr();
            untracked(() => {
                ref.setInput('size', size);
                ref.setInput('color', color);
                ref.setInput('strokeWidth', strokeWidth);
                ref.setInput('secondaryColor', secondaryColor);
                ref.setInput('secondaryOpacity', secondaryOpacity);
                ref.setInput('alt', alt);
                ref.setInput('ariaLabel', ariaLabel);
                ref.setInput('titleAttr', titleAttr);
                if ('weight' in ref.instance) {
                    ref.setInput('weight', weight);
                }
            });
        });
    }
    render(icon) {
        this.vcr.clear();
        let component;
        if (typeof icon === 'string') {
            if (this.registry) {
                component = this.registry[icon];
                if (!component) {
                    console.warn(`[SolarIcon] Icon "${icon}" not found in registry.`);
                }
            }
            else {
                console.warn(`[SolarIcon] No icon registry found. Did you forget to call provideSolarIcons()?`);
            }
        }
        else {
            component = icon;
        }
        if (component) {
            const ref = this.vcr.createComponent(component);
            this.componentRef.set(ref);
        }
        else {
            this.componentRef.set(undefined);
        }
    }
    static ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "22.1.0", ngImport: i0, type: SolarIcon, deps: [], target: i0.ɵɵFactoryTarget.Directive });
    static ɵdir = i0.ɵɵngDeclareDirective({ minVersion: "17.1.0", version: "22.1.0", type: SolarIcon, isStandalone: true, selector: "[solarIcon]", inputs: { solarIcon: { classPropertyName: "solarIcon", publicName: "solarIcon", isSignal: true, isRequired: true, transformFunction: null }, size: { classPropertyName: "size", publicName: "size", isSignal: true, isRequired: false, transformFunction: null }, color: { classPropertyName: "color", publicName: "color", isSignal: true, isRequired: false, transformFunction: null }, strokeWidth: { classPropertyName: "strokeWidth", publicName: "strokeWidth", isSignal: true, isRequired: false, transformFunction: null }, weight: { classPropertyName: "weight", publicName: "weight", isSignal: true, isRequired: false, transformFunction: null }, secondaryColor: { classPropertyName: "secondaryColor", publicName: "secondaryColor", isSignal: true, isRequired: false, transformFunction: null }, secondaryOpacity: { classPropertyName: "secondaryOpacity", publicName: "secondaryOpacity", isSignal: true, isRequired: false, transformFunction: null }, alt: { classPropertyName: "alt", publicName: "alt", isSignal: true, isRequired: false, transformFunction: null }, ariaLabel: { classPropertyName: "ariaLabel", publicName: "ariaLabel", isSignal: true, isRequired: false, transformFunction: null }, titleAttr: { classPropertyName: "titleAttr", publicName: "titleAttr", isSignal: true, isRequired: false, transformFunction: null } }, ngImport: i0 });
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "22.1.0", ngImport: i0, type: SolarIcon, decorators: [{
            type: Directive,
            args: [{
                    selector: '[solarIcon]',
                    standalone: true,
                }]
        }], ctorParameters: () => [], propDecorators: { solarIcon: [{ type: i0.Input, args: [{ isSignal: true, alias: "solarIcon", required: true }] }], size: [{ type: i0.Input, args: [{ isSignal: true, alias: "size", required: false }] }], color: [{ type: i0.Input, args: [{ isSignal: true, alias: "color", required: false }] }], strokeWidth: [{ type: i0.Input, args: [{ isSignal: true, alias: "strokeWidth", required: false }] }], weight: [{ type: i0.Input, args: [{ isSignal: true, alias: "weight", required: false }] }], secondaryColor: [{ type: i0.Input, args: [{ isSignal: true, alias: "secondaryColor", required: false }] }], secondaryOpacity: [{ type: i0.Input, args: [{ isSignal: true, alias: "secondaryOpacity", required: false }] }], alt: [{ type: i0.Input, args: [{ isSignal: true, alias: "alt", required: false }] }], ariaLabel: [{ type: i0.Input, args: [{ isSignal: true, alias: "ariaLabel", required: false }] }], titleAttr: [{ type: i0.Input, args: [{ isSignal: true, alias: "titleAttr", required: false }] }] } });
