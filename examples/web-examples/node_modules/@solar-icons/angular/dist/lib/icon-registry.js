import { InjectionToken } from '@angular/core';
export const SOLAR_ICON_REGISTRY = new InjectionToken('SOLAR_ICON_REGISTRY');
export function provideSolarIcons(registry) {
    return {
        provide: SOLAR_ICON_REGISTRY,
        useValue: registry,
    };
}
